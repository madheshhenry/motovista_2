<?xml version="1.0" encoding="utf-8"?>
<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">

    <!-- Since bg_input_new is a drawable, we should probably reference a color here. -->
    <!-- Let's check colors.xml again. Using surface_card_highlight as a safe default -->
    <solid android:color="@color/surface_card_highlight"/>
    <corners android:radius="12dp" />
    <stroke android:width="1dp" android:color="@color/divider_line" />
</shape>
