<?xml version="1.0" encoding="utf-8"?>
<shape xmlns:android="http://schemas.android.com/apk/res/android" android:shape="rectangle">
    <solid android:color="@color/white" />
    <corners android:radius="14dp" />
    <padding android:left="8dp" android:top="8dp" android:right="8dp" android:bottom="8dp"/>
    <!-- lightweight shadow via stroke (for pre-lollipop subtle effect) -->
    <size android:height="70dp" />

    <solid android:color="@color/input_bg" />
    <corners android:radius="@dimen/corner_lg" />
    <stroke android:width="1dp" android:color="@color/border_color"/>
</shape>
